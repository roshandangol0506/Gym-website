<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animated Form</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: #f6f5f7;
}

.container {
    background-color: #fff;
    border-radius: 10px;
    box-shadow: 0 14px 28px rgba(0, 0, 0, 0.25), 0 10px 10px rgba(0, 0, 0, 0.22);
    position: relative;
    width: 828px;
    max-width: 100%;
    min-height: 540px;
}

.form-container {
    position: absolute;
    top: 0;
    height: 100%;
    line-height: 30px;
    transition: all 0.6s ease-in-out;
    opacity: 0;
    z-index: 1;
    display: none;
}

.sign-up-container {
    margin-top: 40px;
    left: 0;
    width: 50%;
    opacity: 0;
    z-index: 1;
}

.container.right-panel-active .sign-up-container {
    transform: translateX(100%);
    opacity: 1;
    z-index: 5;
    display: block;
}

form {
    display: flex;
    flex-direction: column;
    align-items: center;
}

input[type="text"], 
input[type="email"], 
input[type="number"], 
input[type="file"] {
    width: 60%;
    padding: 5px;
    margin: 5px 0;
    border-radius: 5px;
    border: 1px solid #ccc;
    box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
    transition: border 0.3s, box-shadow 0.3s;
}

input[type="text"]:focus, 
input[type="email"]:focus, 
input[type="number"]:focus, 
input[type="file"]:focus {
    border: 1px solid #007bff;
    box-shadow: 0 0 10px rgba(0, 123, 255, 0.5);
}

button {
    margin-top: 20px;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    background-color: #007bff;
    color: #fff;
    cursor: pointer;
    transition: background-color 0.3s;
}

button:hover {
    background-color: white;
    color: red;
}


.container.right-panel-active .sign-in-container {
    display: none; /* Add this line */
}

.sign-in-container {
    margin: 40px;
    justify-content:space-between;
    left: 0;
    width: 40%;
    z-index: 2;
    opacity: 1;
    display: block; /* Add this line */
}

.container.right-panel-active .sign-in-container {
    transform: translateX(100%);
    z-index: 5;
    opacity: 0;
    display: none; /* Add this line */
}

.overlay-container {
    position: absolute;
    top: 0;
    left: 50%;
    width: 50%;
    height: 100%;
    line-height: 30px;
    overflow: hidden;
    transition: transform 0.6s ease-in-out;
    z-index: 100;
}

.container.right-panel-active .overlay-container {
    transform: translateX(-100%);
}

.overlay {
    background: #ff416c;
    background: -webkit-linear-gradient(to right, #ff4b2b, #ff416c);
    background: linear-gradient(to right, #ff4b2b, #ff416c);
    background-repeat: no-repeat;
    background-size: cover;
    background-position: 0 0;
    color: #fff;
    position: relative;
    left: -100%;
    height: 100%;
    width: 200%;
    transform: translateX(0);
    transition: transform 0.6s ease-in-out;
}

.container.right-panel-active .overlay {
    transform: translateX(50%);
}

.overlay-panel {
    position: absolute;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    padding: 0 40px;
    text-align: center;
    top: 0;
    height: 100%;
    width: 50%;
    transform: translateX(0);
    transition: transform 0.6s ease-in-out;
}

.overlay-left {
    transform: translateX(-20%);
}

.container.right-panel-active .overlay-left {
    transform: translateX(0);
}

.overlay-right {
    right: 0;
    transform: translateX(0);
}

.container.right-panel-active .overlay-right {
    transform: translateX(20%);
}

.ghost {
    background-color: transparent;
    border-color: #fff;
    color: white;
}

.location-button{
    text-align: center;
    justify-content: center;
    margin-top: 20px;
}

.ghosted{
    border-radius: 20px;
    border: 1px solid #ff4b2b;
    color: white;
    
}

button {
    border-radius: 20px;
    border: 1px solid #ff4b2b;
    background-color: #ff4b2b;
    color: white;
    font-size: 12px;
    font-weight: bold;
    padding: 12px 45px;
    text-transform: uppercase;
    transition: transform 80ms ease-in;
    cursor: pointer;
}

button:active {
    transform: scale(0.95);
}

button:focus {
    outline: none;
}

.contact-detail{
    margin-top: 20px;
}


    </style>
</head>

<body>
    <div class="container" id="container">
        <div class="form-container sign-up-container">
            <form action="/uploadProfileImage" method="POST" enctype="multipart/form-data">
                <h1>Create Account</h1>
                <div class="social-container">
                    <a href="#" class="social"><i class="fab fa-facebook-f"></i></a>
                    <a href="#" class="social"><i class="fab fa-google-plus-g"></i></a>
                    <a href="#" class="social"><i class="fab fa-linkedin-in"></i></a>
                </div>
                <span>Registration to join the Gym</span>
                <input type="text" placeholder="Name" required name="name" />
                <input type="email" placeholder="Email" required name="email" />
                <input type="text" placeholder="Location" required name="location" />
                <input type="number" placeholder="Age" required name="age" />
                <input type="number" placeholder="Weight" required name="weight" />
                <input type="number" placeholder="Height" required name="height" />
                <input type="text" placeholder="Phone Number" required name="phoneno" />
                <label>Profile Image</label>
                <input type="file" name="profilepic" accept="image/*" />
                <button type="submit">Signup</button>
            </form>
        </div>
        <div class="form-container sign-in-container">
            <h1>Hello, Friend!</h1>
            <p>Discover the ultimate fitness experience at the best gym in town, where cutting-edge facilities meet expert guidance, helping you achieve your fitness goals in a motivating and supportive environment.</p>
            <div class="contact-detail">
            <p>Location: Bungamati, Lalitpur</p>
            <p>Near Bungamati Buspark</p>
            <p>Contact Us: 9864260404, 9849644940</p>
            <div class="location-button">
            <a href="https://earth.google.com/web/search/Evergreen+Fitness+and+Gym,+Lalitpur+44700" target="_blank" rel="noopener noreferrer"><button class="ghosted">Location</button></a>
            </div>
        </div>
        </div>
        <div class="overlay-container">
            <div class="overlay">
                <div class="overlay-panel overlay-left">
                    <h1>Lets Gym Together!</h1>
                    <p>Please fill the form with Personal Details</p>
                    <button class="ghost" id="signIn">Go Back</button>
                </div>
                <div class="overlay-panel overlay-right">
                    <h1>Welcome to Gym!</h1>
                    <p>Click on Join Button tp create you account.</p>
                    <button class="ghost" id="signUp">Join</button>
                </div>
            </div>
        </div>
    </div>

    <script src="script.js"></script>
</body>

</html>
<script>
    const signUpButton = document.getElementById('signUp');
const signInButton = document.getElementById('signIn');
const container = document.getElementById('container');

signUpButton.addEventListener('click', () => {
    container.classList.add("right-panel-active");
});

signInButton.addEventListener('click', () => {
    container.classList.remove("right-panel-active");
});


</script>